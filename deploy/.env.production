# ══════════════════════════════════════════════════════════════
# ReOrder AI — Production Environment
# Copy to /home/deploy/reorder-ai/.env on the Droplet
# ══════════════════════════════════════════════════════════════

# ── Environment ──────────────────────────────────────────────
ENVIRONMENT=production

# ── Database (PostgreSQL) ────────────────────────────────────
DATABASE_URL=postgresql://reorder:CHANGE_THIS_PASSWORD@localhost:5432/reorder_ai

# ── API ──────────────────────────────────────────────────────
PORT=8000
LOG_LEVEL=WARNING

# CORS: your production domain(s), comma-separated
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ── Security ─────────────────────────────────────────────────
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(48))"
API_SECRET_KEY=CHANGE_THIS_TO_A_RANDOM_STRING

# Rate limits
RATE_LIMIT_DEFAULT=60/minute
RATE_LIMIT_SYNC=10/minute

# ── AWS Forecast ─────────────────────────────────────────────
FORECAST_PROVIDER=amazon_forecast
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=CHANGE_THIS
AWS_SECRET_ACCESS_KEY=CHANGE_THIS

# Auto-refresh forecasts every 24 hours
FORECAST_REFRESH_HOURS=24
