version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
      - ./data:/app/data
    restart: always

  # Optional: Vector DB like Chroma or Qdrant could be added here
  # chromadb:
  #   image: chromadb/chroma
  #   ports:
  #     - "8001:8000"
