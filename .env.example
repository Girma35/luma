# ReOrder AI — copy to .env and fill in values
#
# Store credentials (Shopify / WooCommerce) are provided by users
# through the dashboard "Connect Store" flow — not set here.

# ── Environment ──────────────────────────────────────────────
# development | staging | production
ENVIRONMENT=development

# ── Database ─────────────────────────────────────────────────
# SQLite (default, good for local dev):
DATABASE_URL=sqlite:///./reorder_ai.db
# PostgreSQL (recommended for production):
# DATABASE_URL=postgresql://user:password@localhost:5432/reorder_ai

# ── API ──────────────────────────────────────────────────────
# PORT=8000
# LOG_LEVEL=INFO

# CORS: comma-separated origins that can call the API
CORS_ORIGINS=http://localhost:4321,http://localhost:3000,http://127.0.0.1:4321

# ── Security ─────────────────────────────────────────────────
# API key for protected endpoints (sync, pipeline, forecast run, write ops).
# Leave empty to disable auth (dev mode). Set a strong random string in production.
# API_SECRET_KEY=your-random-secret-key-here

# Rate limits (format: "count/period" e.g. "60/minute", "1000/hour")
# RATE_LIMIT_DEFAULT=60/minute
# RATE_LIMIT_SYNC=10/minute

# ── AWS Forecast ─────────────────────────────────────────────
# Provider: "simple" (local math, free) or "amazon_forecast" (AWS ML)
FORECAST_PROVIDER=simple
# Auto-refresh interval (hours). 0 = disabled.
# FORECAST_REFRESH_HOURS=24

# Only needed if FORECAST_PROVIDER=amazon_forecast
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=AKIAxxxxxxxxxxxxxxxx
# AWS_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
