# ReOrder AI Dashboard — copy to .env and fill in

# Better Auth (required for login/signup)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-32-char-minimum-secret-here
# Base URL of this app (default works for local dev)
BETTER_AUTH_URL=http://localhost:4321

# Optional: Redis for session storage (sessions persist across restarts; returning users stay logged in)
# REDIS_URL=redis://localhost:6379

# Shopify app (for Connect Shopify OAuth)
# Create an app in Shopify Partner Dashboard; set redirect URL to: {BETTER_AUTH_URL}/connect/shopify/callback
# SHOPIFY_CLIENT_ID=your_client_id
# SHOPIFY_CLIENT_SECRET=your_client_secret

# Optional: API runs on a different host/port
# PUBLIC_API_URL=http://localhost:8000

# Paddle billing — PAUSED (set PADDLE_BILLING_ENABLED = true in middleware when ready)
# Client-side token: Paddle > Developer tools > Authentication
# PUBLIC_PADDLE_CLIENT_TOKEN=live_xxxxxxxx  # or test_xxx for sandbox
# Price ID: create product/price in Paddle
# PUBLIC_PADDLE_PRICE_ID=pri_xxxxxxxx
# Webhook: Paddle > Notifications > destination {BETTER_AUTH_URL}/api/webhooks/paddle; subscribe to subscription.* events
# PADDLE_WEBHOOK_SECRET=your-webhook-endpoint-secret
